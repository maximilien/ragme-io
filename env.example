# RAGme AI Configuration
# Copy this file to .env and update with your values

# Frontend options
APPLICATION_NAME="Your RAGme App Name" # this is used in the frontend top bar
APPLICATION_VERSION="0.1.0"
APPLICATION_TITLE="Your RAGme app title"
APPLICATION_DESCRIPTION="Your RAGme app description."

# Vector Database Configuration
# Options: "milvus" (default, local), "weaviate" (cloud), or "weaviate-local" (local Podman)
VECTOR_DB_TYPE=milvus
VECTOR_DB_COLLECTION_NAME=my-collection

# Weaviate Cloud Configuration (only needed if VECTOR_DB_TYPE=weaviate)
# Get these from https://console.weaviate.cloud/
WEAVIATE_API_KEY=your-weaviate-api-key-here
WEAVIATE_URL=https://your-cluster.weaviate.network

# Local Weaviate Configuration (only needed if VECTOR_DB_TYPE=weaviate-local)
# Start local Weaviate with: ./tools/weaviate-local.sh start
WEAVIATE_LOCAL_URL=http://localhost:8080

# Milvus Configuration (only needed if VECTOR_DB_TYPE=milvus)
# For local development, use the default file-based database
MILVUS_URI=milvus_demo.db
# MILVUS_TOKEN=your-milvus-token-here  # Only needed for cloud Milvus

# OpenAI Configuration (for LLM queries)
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Custom ports for services

# RAGME_API_PORT=8021
# RAGME_MCP_PORT=8022
# RAGME_FRONTEND_PORT=3020 